# created by virtualenv automatically
*# ----------------------------------------------------------------------
# 1. Virtual Environments
# These are locally created environments and should not be tracked by Git.
# ----------------------------------------------------------------------
.venv
venv
ENV

# ----------------------------------------------------------------------
# 2. IDE / System Files
# Files generated by the IDE (e.g., PyCharm) or the operating system.
# ----------------------------------------------------------------------
.idea/
.DS_Store
*.pyc
__pycache__/

# ----------------------------------------------------------------------
# 3. Output and Temporary Files (CRITICAL)
# Files generated when the code runs (outputs, temporary chunks).
# ----------------------------------------------------------------------

# Output text files (e.g., summary.txt, meeting_log.txt)
*.txt

# Temporary audio files created during chunk processing
temp_chunk_*.wav

# ----------------------------------------------------------------------
# 4. Large Audio Files / Private Data
# Exclude large audio test files to keep the repository small.
# ----------------------------------------------------------------------
# Exclude all contents of the Voices directory (if they are large/private)
Voices/
